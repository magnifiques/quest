"use server";

export const signIn = async () => {
  try {
    //Mutation / Database / Data fetch
  } catch (error) {
    console.error(error);
  }
};

export const signUp = async () => {
  try {
    //Mutation / Database / Data fetch
  } catch (error) {
    console.error(error);
  }
};
